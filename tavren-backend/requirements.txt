fastapi==0.105.0
uvicorn[standard]==0.24.0 # Includes extras like httptools, websockets
SQLAlchemy==2.0.23
pydantic==2.3.0
pydantic-settings # For centralized configuration
python-dotenv==1.0.0 # For environment variable loading
greenlet==3.1.1 # Often needed by SQLAlchemy
requests # For the payout processor script
python-multipart # Required for OAuth2 password flow form data
slowapi # For rate limiting
cryptography==42.0.0 # For encryption/decryption of data packages

# LLM and Embedding Dependencies
aiohttp==3.9.3 # For async HTTP requests to LLM APIs
sentence-transformers==2.3.1 # For local embedding processing
numpy>=1.24.0 # Required for vector operations
pgvector # For PostgreSQL vector extensions (if using Postgres)
tiktoken==0.6.0 # For token counting and text chunking

# Caching and performance
redis>=5.0.0 # For Redis caching
aioredis>=2.0.0 # For async Redis operations
cachetools>=5.0.0 # For in-memory caching

# Testing dependencies
pytest
pytest-asyncio # For testing async code
httpx # For testing FastAPI endpoints async

# Database drivers
asyncpg # For PostgreSQL async connection
aiosqlite # For SQLite async connection

# Linting
ruff

# Add other specific dependencies for run_payout_processor.py if needed 